{
  "Parameters": {
    "SupabaseUrl": "${SUPABASE_URL}",
    "SupabaseKey": "${SUPABASE_KEY}",
    "TelegramLambdaName": "${TELEGRAM_LAMBDA_NAME}",
    "AudioLambdaName": "${AUDIO_LAMBDA_NAME}",
    "SQSQueueUrl": "${SQS_QUEUE_URL}"
  },
  "PodcastSchedulerFunction": {
    "SUPABASE_URL": "${SUPABASE_URL}",
    "SUPABASE_KEY": "${SUPABASE_KEY}",
    "TELEGRAM_LAMBDA_NAME": "${TELEGRAM_LAMBDA_NAME}",
    "AUDIO_LAMBDA_NAME": "${AUDIO_LAMBDA_NAME}",
    "SQS_QUEUE_URL": "${SQS_QUEUE_URL}",
    "LOG_LEVEL": "DEBUG",
    "AWS_SAM_LOCAL": "false"
  }
} 